#include "holberton.h"
#include <stdlib.h>
/**
 * str_concat - concatenates 2 strings
 * @s1: 1st string
 * @s2:2nd string
 *
 * Return: char
 */
char *str_concat(char *s1, char *s2)
{
	char *concat_str;
	int index, concat_index = 0, len = 0;

	if (s1 == NULL)
		s1 = "";

	if (s2 == NULL)
		s2 = "";
}
