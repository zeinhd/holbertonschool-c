#include "holberton.h"

/**
 *print_rev - print string in reverse
 *@s: string
 *
 *Return (VOID)
 */
void print_rev(char *s)
{
	int i, Sl;

	Sl = _strlen(s);

	for (i = Sl - 1; i >= 0; i--)
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
