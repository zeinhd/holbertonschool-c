#include "holberton.h"

/**
 *strcpy - copy dest to src
 *@dest: destination
 *@src: source
 *
 *Return: pointer to dest
 */
char *_strcpy(char *dest, char *src)
{
	int i = 0;
	char *destValue = dest;

	while (src[i])
	{
		dest[i] = src[i];
		i++;
	}
	return (destValue);
}
